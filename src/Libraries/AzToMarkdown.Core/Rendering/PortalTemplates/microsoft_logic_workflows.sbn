{{~ # Auto-generated by AzResourceDetailsDownloader --generate-templates for Microsoft.Logic/workflows. ~}}
{{~ # Requires these custom Scriban functions registered on the rendering engine: ~}}
{{~ # portal_bool_enabled, portal_bool_yesno, portal_timestamp, region_display_name — see TemplateFunctions.cs. ~}}
{{~ # Rows marked TODO could not be mechanically resolved against the captured example — see config/portal-field-recipes.json for why. ~}}
# {{ model.name }}

| Property | Value |
|---|---|
| **Resource group** | {{ model.resource_group }} |
| **Location** | {{ model.location | region_display_name }} |
<!-- "Subscription" omitted: tenant/subscription identity, already in frontmatter -->
<!-- "Subscription ID" omitted: tenant/subscription identity, already in frontmatter -->
| **Workflow URL** | <!-- TODO (Unresolved): fetched from a separate callback-URL API, not present in the resource GET body — different-API-surface, not a parsing gap (confirmed live on Logic/workflows) Captured example: "--" --> *Not available from captured ARM metadata.* |
| **Definition** | {{ model.logic_workflow_definition_label }} |
| **Status** | *See the Azure Portal for current status.* |
| **Runs last 24 hours** | <!-- TODO (Unresolved): fetched live from Azure Monitor metrics (RunsSucceeded/RunsFailed), not present anywhere in the resource GET body — different-API-surface, not a parsing gap (confirmed live on Logic/workflows) Captured example: "0 successful, 0 failed" --> *Not available from captured ARM metadata.* |
| **Integration Account** | {{ model.logic_integration_account_label }} |
| **Workflow Type** | {{ model.logic_workflow_type_label }} |
