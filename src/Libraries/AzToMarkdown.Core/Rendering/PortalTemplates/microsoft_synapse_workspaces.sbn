{{~ # Auto-generated by AzResourceDetailsDownloader --generate-templates for Microsoft.Synapse/workspaces. ~}}
{{~ # Requires these custom Scriban functions registered on the rendering engine: ~}}
{{~ # portal_bool_enabled, portal_bool_yesno, portal_timestamp, region_display_name — see TemplateFunctions.cs. ~}}
{{~ # Rows marked TODO could not be mechanically resolved against the captured example — see config/portal-field-recipes.json for why. ~}}
# {{ model.name }}

| Property | Value |
|---|---|
| **Resource group** | {{ model.resource_group }} |
| **Status** | *See the Azure Portal for current status.* |
| **Location** | {{ model.location | region_display_name }} |
<!-- "Subscription" omitted: tenant/subscription identity, already in frontmatter -->
<!-- "Subscription ID" omitted: tenant/subscription identity, already in frontmatter -->
| **Managed virtual network** | <!-- TODO (NeedsReview): Value matches (boolean→friendly-word) but the property name is only weakly related to the label (similarity 0.33) — verify by hand before trusting (4 addressable value-matching leaves, none strongly name-related to the label). Captured example: "No" --> *Not available from captured ARM metadata.* |
| **Managed Identity object ID** | <!-- TODO (NotAddressable): Traceable via 'identity.principalid' (exact value match), but that's outside properties.*/kind/name/id/location — not reachable from a template. Captured example: "e8ad3ab8-145c-497d-b464-3e86c31c93dc" --> *Not available from captured ARM metadata.* |
| **Workspace web URL** | <!-- TODO (NeedsReview): Value matches (exact value match) but the property name is only weakly related to the label (similarity 0.33) — verify by hand before trusting. Captured example: "https://web.azuresynapse.net?workspace=%2fsubscriptions%2f00000000-0000-0000-0000-000000000000%2fresourceGroups%2frg-ardl-bb17cace9b9db143%2fproviders%2fMicrosoft.Synapse%2fworkspaces%2fsynbhgh-pfl" --> *Not available from captured ARM metadata.* |
| **Primary ADLS Gen2 account URL** | <!-- TODO (NeedsReview): Value matches (exact value match) but the property name is only weakly related to the label (similarity 0.40) — verify by hand before trusting. Captured example: "https://sthlzapzqb.dfs.core.windows.net" --> *Not available from captured ARM metadata.* |
| **Primary ADLS Gen2 file system** | <!-- TODO (NeedsReview): Value matches (exact value match) but the property name is only weakly related to the label (similarity 0.20) — verify by hand before trusting. Captured example: "synapsefs" --> *Not available from captured ARM metadata.* |
| **SQL admin username** | {{ model.props.sqladministratorlogin }} |
<!-- "SQL Microsoft Entra admin" omitted: tenant/subscription identity, already in frontmatter -->
| **Dedicated SQL endpoint** | {{ model.props.connectivityendpoints.sql }} |
| **Serverless SQL endpoint** | {{ model.props.connectivityendpoints.sqlondemand }} |
| **Development endpoint** | {{ model.props.connectivityendpoints.dev }} |
