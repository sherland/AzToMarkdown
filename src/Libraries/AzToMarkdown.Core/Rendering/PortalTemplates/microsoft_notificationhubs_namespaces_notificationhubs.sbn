{{~ # Auto-generated by AzResourceDetailsDownloader --generate-templates for Microsoft.NotificationHubs/namespaces/notificationHubs. ~}}
{{~ # Requires these custom Scriban functions registered on the rendering engine: ~}}
{{~ # portal_bool_enabled, portal_bool_yesno, portal_timestamp, region_display_name — see TemplateFunctions.cs. ~}}
{{~ # Rows marked TODO could not be mechanically resolved against the captured example — see config/portal-field-recipes.json for why. ~}}
# {{ model.name }}

| Property | Value |
|---|---|
| **Resource group** | {{ model.resource_group }} |
| **Status** | *See the Azure Portal for current status.* |
| **Location** | {{ model.location | region_display_name }} |
<!-- "Subscription" omitted: tenant/subscription identity, already in frontmatter -->
<!-- "Subscription ID" omitted: tenant/subscription identity, already in frontmatter -->
| **Name** | {{ model.name }} |
| **Namespace** | <!-- TODO (Unresolved): No value match found anywhere in this capture — investigate before adding to any known table (could be a genuine EssentialsExtractor/redaction bug, a portal empty-state placeholder like "---", or a new different-API-surface/composite case). Captured example: "nhnsmuv5af-w" --> *Not available from captured ARM metadata.* |
| **Pricing Tier** | <!-- TODO (ShortcutMismatch): portal shows "Free" but no sku object found at root or properties.sku Captured example: "Free" --> *Not available from captured ARM metadata.* |
| **Paired recovery region** | <!-- TODO (Unresolved): composite/derived — no single backing property (see NonTraceableLabels comments) Captured example: "Norway West" --> *Not available from captured ARM metadata.* |
| **Availability zones** | <!-- TODO (Unresolved): no backing property found in prior corpus inspection Captured example: "Enabled" --> *Not available from captured ARM metadata.* |
