{{~ # Auto-generated by AzResourceDetailsDownloader --generate-templates for Microsoft.ServiceBus/namespaces/topics/subscriptions. ~}}
{{~ # Requires these custom Scriban functions registered on the rendering engine: ~}}
{{~ # portal_bool_enabled, portal_bool_yesno, portal_timestamp, region_display_name — see TemplateFunctions.cs. ~}}
{{~ # Rows marked TODO could not be mechanically resolved against the captured example — see config/portal-field-recipes.json for why. ~}}
# {{ model.name }}

| Property | Value |
|---|---|
| **Namespace** | <!-- TODO (Unresolved): No value match found anywhere in this capture — investigate before adding to any known table (could be a genuine EssentialsExtractor/redaction bug, a portal empty-state placeholder like "---", or a new different-API-surface/composite case). Captured example: "sbg0yna0pz" --> *Not available from captured ARM metadata.* |
| **Topic** | <!-- TODO (Unresolved): No value match found anywhere in this capture — investigate before adding to any known table (could be a genuine EssentialsExtractor/redaction bug, a portal empty-state placeholder like "---", or a new different-API-surface/composite case). Captured example: "topicvj-o3e" --> *Not available from captured ARM metadata.* |
| **Status** | {{ model.props.status }} <sub>(as of last sync)</sub> |
| **Forward messages to** | <!-- TODO (NeedsReview): Value matches (boolean→friendly-word) but the property name is only weakly related to the label (similarity 0.33) — verify by hand before trusting (3 addressable value-matching leaves, none strongly name-related to the label). Captured example: "Disabled" --> *Not available from captured ARM metadata.* |
| **Created** | {{ model.props.createdat | portal_timestamp }} |
| **Updated** | {{ model.props.updatedat | portal_timestamp }} <sub>(as of last sync)</sub> |
| **Sessions** | {{ model.props.requiressession | portal_bool_enabled }} |
| **Dead lettering** | <!-- TODO (Unresolved): composite of 2 booleans combined into one sentence Captured example: "Disabled on message expiration, enabled on filter exception" --> *Not available from captured ARM metadata.* |
