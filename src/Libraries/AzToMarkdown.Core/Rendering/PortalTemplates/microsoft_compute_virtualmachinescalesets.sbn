{{~ # Auto-generated by AzResourceDetailsDownloader --generate-templates for Microsoft.Compute/virtualMachineScaleSets. ~}}
{{~ # Requires these custom Scriban functions registered on the rendering engine: ~}}
{{~ # portal_bool_enabled, portal_bool_yesno, portal_timestamp, region_display_name — see TemplateFunctions.cs. ~}}
{{~ # Rows marked TODO could not be mechanically resolved against the captured example — see config/portal-field-recipes.json for why. ~}}
# {{ model.name }}

| Property | Value |
|---|---|
| **Resource group** | {{ model.resource_group }} |
| **Status** | *See the Azure Portal for current status.* |
<!-- "Subscription" omitted: tenant/subscription identity, already in frontmatter -->
| **Location** | {{ model.location | region_display_name }} |
<!-- "Subscription ID" omitted: tenant/subscription identity, already in frontmatter -->
| **Operating system** | <!-- TODO (NeedsReview): Value matches (exact value match) but the property name is only weakly related to the label (similarity 0.00) — verify by hand before trusting. Captured example: "Linux" --> *Not available from captured ARM metadata.* |
| **Size** | <!-- TODO (Unresolved): No value match found anywhere in this capture — investigate before adding to any known table (could be a genuine EssentialsExtractor/redaction bug, a portal empty-state placeholder like "---", or a new different-API-surface/composite case). Captured example: "Standard_D2s_v5 (1 instance)" --> *Not available from captured ARM metadata.* |
| **Public IP address** | <!-- TODO (Unresolved): composite/derived — no single backing property (see NonTraceableLabels comments) Captured example: "-" --> *Not available from captured ARM metadata.* |
| **Public IP address (IPv6)** | <!-- TODO (Unresolved): No value match found anywhere in this capture — investigate before adding to any known table (could be a genuine EssentialsExtractor/redaction bug, a portal empty-state placeholder like "---", or a new different-API-surface/composite case). Captured example: "-" --> *Not available from captured ARM metadata.* |
| **Virtual network/subnet** | <!-- TODO (Unresolved): composite/derived — no single backing property (see NonTraceableLabels comments) Captured example: "vnetd7-ts-wt/default" --> *Not available from captured ARM metadata.* |
| **Orchestration mode** | {{ model.props.orchestrationmode }} |
| **Time created** | {{ model.props.timecreated | portal_timestamp }} |
