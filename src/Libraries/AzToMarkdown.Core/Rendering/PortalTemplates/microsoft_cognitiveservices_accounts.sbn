{{~ # Auto-generated by AzResourceDetailsDownloader --generate-templates for Microsoft.CognitiveServices/accounts. ~}}
{{~ # Requires these custom Scriban functions registered on the rendering engine: ~}}
{{~ # portal_bool_enabled, portal_bool_yesno, portal_timestamp, region_display_name — see TemplateFunctions.cs. ~}}
{{~ # Rows marked TODO could not be mechanically resolved against the captured example — see config/portal-field-recipes.json for why. ~}}
# {{ model.name }}

| Property | Value |
|---|---|
| **Resource group** | {{ model.resource_group }} |
| **Status** | *See the Azure Portal for current status.* |
| **Location** | {{ model.location | region_display_name }} |
<!-- "Subscription" omitted: tenant/subscription identity, already in frontmatter -->
<!-- "Subscription ID" omitted: tenant/subscription identity, already in frontmatter -->
| **API Kind** | {{ model.props.kind }} |
| **Pricing tier** | <!-- TODO (ShortcutMismatch): Computed "S0" does NOT match portal "Standard" for this type — do not trust this shortcut here. Captured example: "Standard" --> *Not available from captured ARM metadata.* |
| **Endpoint** | <!-- TODO (Ambiguous): Tied with model.props.endpoints.face (similarity 1.00) — pick manually. Captured example: "https://ardlcog8l2-q-ck.cognitiveservices.azure.com/" --> *Not available from captured ARM metadata.* |
| **Autoscale** | <!-- TODO (NeedsReview): Value matches (boolean→friendly-word) but the property name is only weakly related to the label (similarity 0.00) — verify by hand before trusting. Captured example: "Disabled" --> *Not available from captured ARM metadata.* |
