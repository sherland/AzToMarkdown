{{~ # Auto-generated by AzResourceDetailsDownloader --generate-templates for Microsoft.ServiceBus/namespaces/topics. ~}}
{{~ # Requires these custom Scriban functions registered on the rendering engine: ~}}
{{~ # portal_bool_enabled, portal_bool_yesno, portal_timestamp, region_display_name — see TemplateFunctions.cs. ~}}
{{~ # Rows marked TODO could not be mechanically resolved against the captured example — see config/portal-field-recipes.json for why. ~}}
# {{ model.name }}

| Property | Value |
|---|---|
| **Namespace** | <!-- TODO (Unresolved): No value match found anywhere in this capture — investigate before adding to any known table (could be a genuine EssentialsExtractor/redaction bug, a portal empty-state placeholder like "---", or a new different-API-surface/composite case). Captured example: "sbalnbuxtr" --> *Not available from captured ARM metadata.* |
| **Status** | {{ model.props.status }} <sub>(as of last sync)</sub> |
| **Partitioning** | {{ model.props.enablepartitioning | portal_bool_enabled }} |
| **Duplicate detection** | {{ model.props.requiresduplicatedetection | portal_bool_enabled }} |
| **Topic URL** | <!-- TODO (Unresolved): No value match found anywhere in this capture — investigate before adding to any known table (could be a genuine EssentialsExtractor/redaction bug, a portal empty-state placeholder like "---", or a new different-API-surface/composite case). Captured example: "https://sbalnbuxtr.servicebus.windows.net/topicv0fcfy" --> *Not available from captured ARM metadata.* |
| **Created** | {{ model.props.createdat | portal_timestamp }} |
| **Updated** | {{ model.props.updatedat | portal_timestamp }} <sub>(as of last sync)</sub> |
| **Support ordering** | {{ model.props.supportordering | portal_bool_enabled }} |
