{{~ # Auto-generated by AzResourceDetailsDownloader --generate-templates for Microsoft.DataMigration/services. ~}}
{{~ # Requires these custom Scriban functions registered on the rendering engine: ~}}
{{~ # portal_bool_enabled, portal_bool_yesno, portal_timestamp, region_display_name — see TemplateFunctions.cs. ~}}
{{~ # Rows marked TODO could not be mechanically resolved against the captured example — see config/portal-field-recipes.json for why. ~}}
# {{ model.name }}

| Property | Value |
|---|---|
| **Resource group** | {{ model.resource_group }} |
| **Virtual network & IP Address** | <!-- TODO (Unresolved): composite/derived — no single backing property (see NonTraceableLabels comments) Captured example: "vnet46-vasrl/subnets/default 10.81.0.4" --> *Not available from captured ARM metadata.* |
<!-- "Subscription" omitted: tenant/subscription identity, already in frontmatter -->
| **SKU** | <!-- TODO (ShortcutMismatch): Computed "Premium (Premium_4vCores)" does NOT match portal "Premium: 4 vCores" for this type — do not trust this shortcut here. Captured example: "Premium: 4 vCores" --> *Not available from captured ARM metadata.* |
| **Status** | *See the Azure Portal for current status.* |
| **Location** | {{ model.location | region_display_name }} |
<!-- "Subscription ID" omitted: tenant/subscription identity, already in frontmatter -->
| **Service/UI Version** | <!-- TODO (Unresolved): composite/derived — no single backing property (see NonTraceableLabels comments) Captured example: "5.1.6992.1/5.1.7033.1" --> *Not available from captured ARM metadata.* |
