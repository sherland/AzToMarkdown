{{~ # Auto-generated by AzResourceDetailsDownloader --generate-templates for Microsoft.ContainerService/ManagedClusters. ~}}
{{~ # Requires these custom Scriban functions registered on the rendering engine: ~}}
{{~ # portal_bool_enabled, portal_bool_yesno, portal_timestamp, region_display_name — see TemplateFunctions.cs. ~}}
{{~ # Rows marked TODO could not be mechanically resolved against the captured example — see config/portal-field-recipes.json for why. ~}}
# {{ model.name }}

| Property | Value |
|---|---|
| **Resource group** | {{ model.resource_group }} |
| **Power state** | {{ model.aks_power_state_label }} |
| **Cluster operation status** | {{ model.aks_cluster_operation_status_label }} |
<!-- "Subscription" omitted: tenant/subscription identity, already in frontmatter -->
| **Location** | {{ model.location | region_display_name }} |
<!-- "Subscription ID" omitted: tenant/subscription identity, already in frontmatter -->
| **Kubernetes version** | {{ model.props.currentkubernetesversion }} |
| **API server address** | {{ model.aks_api_server_address_label }} |
| **SKU** | {{ model.sku_name }} |
| **Pricing tier** | {{ model.sku_tier }} |
| **Network configuration** | {{ model.aks_network_configuration_label }} |
| **Node pools** | {{ model.aks_node_pools_label }} |
| **Created time** | <!-- TODO (Unresolved): AKS clusters fetch this from a SEPARATE API call, not the resource GET body this tool captures at all — genuinely a different-API-surface case, not a parsing gap (confirmed live on ContainerService/ManagedClusters; unrelated to the AKS/Storage "Date created"/"Created time" cases already in NonTraceableLabels) Captured example: "16. august 2026 kl. 16:05" --> *Not available from captured ARM metadata.* |
