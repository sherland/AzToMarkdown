{{~ # Auto-generated by AzResourceDetailsDownloader --generate-templates for Microsoft.Batch/batchAccounts. ~}}
{{~ # Requires these custom Scriban functions registered on the rendering engine: ~}}
{{~ # portal_bool_enabled, portal_bool_yesno, portal_timestamp, region_display_name — see TemplateFunctions.cs. ~}}
{{~ # Rows marked TODO could not be mechanically resolved against the captured example — see config/portal-field-recipes.json for why. ~}}
# {{ model.name }}

| Property | Value |
|---|---|
| **Resource group** | {{ model.resource_group }} |
| **Status** | *See the Azure Portal for current status.* |
| **Location** | {{ model.location | region_display_name }} |
<!-- "Subscription" omitted: tenant/subscription identity, already in frontmatter -->
<!-- "Subscription ID" omitted: tenant/subscription identity, already in frontmatter -->
| **Account endpoint** | {{ model.props.accountendpoint }} |
| **Node management endpoint** | {{ model.props.nodemanagementendpoint }} |
| **Identity type** | <!-- TODO (NotAddressable): Traceable via 'identity.type' (exact value match), but that's outside properties.*/kind/name/id/location — not reachable from a template. Captured example: "None" --> *Not available from captured ARM metadata.* |
| **Public network access** | <!-- TODO (Unresolved): composite of publicNetworkAccess + networkAcls.defaultAction Captured example: "All networks" --> *Not available from captured ARM metadata.* |
| **Pool allocation mode** | {{ model.props.poolallocationmode | string.capitalize }} |
