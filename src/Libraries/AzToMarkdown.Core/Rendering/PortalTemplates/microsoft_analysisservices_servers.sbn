{{~ # Auto-generated by AzResourceDetailsDownloader --generate-templates for Microsoft.AnalysisServices/servers. ~}}
{{~ # Requires these custom Scriban functions registered on the rendering engine: ~}}
{{~ # portal_bool_enabled, portal_bool_yesno, portal_timestamp, region_display_name — see TemplateFunctions.cs. ~}}
{{~ # Rows marked TODO could not be mechanically resolved against the captured example — see config/portal-field-recipes.json for why. ~}}
# {{ model.name }}

| Property | Value |
|---|---|
<!-- "Subscription name" omitted: tenant/subscription identity, already in frontmatter -->
| **Resource group** | {{ model.resource_group }} |
| **Status** | *See the Azure Portal for current status.* |
| **Location** | {{ model.location | region_display_name }} |
<!-- "Subscription ID" omitted: tenant/subscription identity, already in frontmatter -->
| **Server name** | <!-- TODO (Unresolved): composite/derived — no single backing property (see NonTraceableLabels comments) Captured example: "asazure://northeurope.asazure.windows.net/asrivqjljz" --> *Not available from captured ARM metadata.* |
| **Management Server Name** | <!-- TODO (Unresolved): composite/derived — no single backing property (see NonTraceableLabels comments) Captured example: "asazure://northeurope.asazure.windows.net/asrivqjljz:rw" --> *Not available from captured ARM metadata.* |
| **Pricing tier** | {{ model.sku_name }} |
