{{~ # Auto-generated by AzResourceDetailsDownloader --generate-templates for Microsoft.Network/networkManagers. ~}}
{{~ # Requires these custom Scriban functions registered on the rendering engine: ~}}
{{~ # portal_bool_enabled, portal_bool_yesno, portal_timestamp, region_display_name — see TemplateFunctions.cs. ~}}
{{~ # Rows marked TODO could not be mechanically resolved against the captured example — see config/portal-field-recipes.json for why. ~}}
# {{ model.name }}

| Property | Value |
|---|---|
<!-- "Subscription" omitted: tenant/subscription identity, already in frontmatter -->
| **Resource group** | {{ model.resource_group }} |
| **Location** | {{ model.location | region_display_name }} |
<!-- "Subscription ID" omitted: tenant/subscription identity, already in frontmatter -->
| **Enabled features** | <!-- TODO (Unresolved): No value match found anywhere in this capture — investigate before adding to any known table (could be a genuine EssentialsExtractor/redaction bug, a portal empty-state placeholder like "---", or a new different-API-surface/composite case). Captured example: "Connectivity, SecurityAdmin" --> *Not available from captured ARM metadata.* |
| **Description** | <!-- TODO (Unresolved): No value match found anywhere in this capture — investigate before adding to any known table (could be a genuine EssentialsExtractor/redaction bug, a portal empty-state placeholder like "---", or a new different-API-surface/composite case). Captured example: "--" --> *Not available from captured ARM metadata.* |
| **Scope (In-tenant)** | <!-- TODO (Unresolved): composite/derived — no single backing property (see NonTraceableLabels comments) Captured example: "Management groups (0); Subscriptions (1)" --> *Not available from captured ARM metadata.* |
| **Scope (Cross-tenant)** | <!-- TODO (Unresolved): composite/derived — no single backing property (see NonTraceableLabels comments) Captured example: "Management groups (0); Subscriptions (0)" --> *Not available from captured ARM metadata.* |
| **Provisioning state** | {{ model.props.provisioningstate }} <sub>(as of last sync)</sub> |
| **Time created** | <!-- TODO (NotAddressable): Traceable via 'systemdata.createdat', but that's outside properties.* — not reachable as model.props.*. Would need the renderer to expose that section, or the label stays manual. Captured example: "2026-08-14 10:51:50" --> *Not available from captured ARM metadata.* |
| **Time modified** | <!-- TODO (NotAddressable): Traceable via 'systemdata.createdat', but that's outside properties.* — not reachable as model.props.*. Would need the renderer to expose that section, or the label stays manual. Captured example: "2026-08-14 10:51:50" --> *Not available from captured ARM metadata.* |
