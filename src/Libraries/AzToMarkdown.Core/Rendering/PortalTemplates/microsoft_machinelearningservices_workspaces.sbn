{{~ # Auto-generated by AzResourceDetailsDownloader --generate-templates for Microsoft.MachineLearningServices/workspaces. ~}}
{{~ # Requires these custom Scriban functions registered on the rendering engine: ~}}
{{~ # portal_bool_enabled, portal_bool_yesno, portal_timestamp, region_display_name — see TemplateFunctions.cs. ~}}
{{~ # Rows marked TODO could not be mechanically resolved against the captured example — see config/portal-field-recipes.json for why. ~}}
# {{ model.name }}

| Property | Value |
|---|---|
| **Resource group** | {{ model.resource_group }} |
| **Location** | {{ model.location | region_display_name }} |
<!-- "Subscription" omitted: tenant/subscription identity, already in frontmatter -->
| **Storage** | <!-- TODO (Unresolved): No value match found anywhere in this capture — investigate before adding to any known table (could be a genuine EssentialsExtractor/redaction bug, a portal empty-state placeholder like "---", or a new different-API-surface/composite case). Captured example: "sth6t6yrwo" --> *Not available from captured ARM metadata.* |
| **Studio web URL** | <!-- TODO (Unresolved): composite/derived — no single backing property (see NonTraceableLabels comments) Captured example: "https://ml.azure.com?tid=11111111-1111-1111-1111-111111111111&wsid=/subscriptions/00000000-0000-0000-0000-000000000000/resourcegroups/rg-ardl-f7e533e01923b0a9/providers/Microsoft.MachineLearningServices/workspaces/mlwrc-5ff4m" --> *Not available from captured ARM metadata.* |
| **Container Registry** | <!-- TODO (Unresolved): No value match found anywhere in this capture — investigate before adding to any known table (could be a genuine EssentialsExtractor/redaction bug, a portal empty-state placeholder like "---", or a new different-API-surface/composite case). Captured example: "..." --> *Not available from captured ARM metadata.* |
| **Key Vault** | <!-- TODO (Unresolved): No value match found anywhere in this capture — investigate before adding to any known table (could be a genuine EssentialsExtractor/redaction bug, a portal empty-state placeholder like "---", or a new different-API-surface/composite case). Captured example: "kv7s29-71a" --> *Not available from captured ARM metadata.* |
| **Application Insights** | <!-- TODO (Unresolved): No value match found anywhere in this capture — investigate before adding to any known table (could be a genuine EssentialsExtractor/redaction bug, a portal empty-state placeholder like "---", or a new different-API-surface/composite case). Captured example: "ai2rhbhwk1" --> *Not available from captured ARM metadata.* |
| **Provisioning State** | {{ model.props.provisioningstate }} <sub>(as of last sync)</sub> |
| **MLflow tracking URI** | {{ model.props.mlflowtrackinguri }} |
