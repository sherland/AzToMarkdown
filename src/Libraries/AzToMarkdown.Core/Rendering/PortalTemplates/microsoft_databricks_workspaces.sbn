{{~ # Auto-generated by AzResourceDetailsDownloader --generate-templates for Microsoft.Databricks/workspaces. ~}}
{{~ # Requires these custom Scriban functions registered on the rendering engine: ~}}
{{~ # portal_bool_enabled, portal_bool_yesno, portal_timestamp, region_display_name — see TemplateFunctions.cs. ~}}
{{~ # Rows marked TODO could not be mechanically resolved against the captured example — see config/portal-field-recipes.json for why. ~}}
# {{ model.name }}

| Property | Value |
|---|---|
| **Status** | *See the Azure Portal for current status.* |
| **Resource group** | {{ model.resource_group }} |
| **Location** | {{ model.location | region_display_name }} |
<!-- "Subscription" omitted: tenant/subscription identity, already in frontmatter -->
<!-- "Subscription ID" omitted: tenant/subscription identity, already in frontmatter -->
| **Workspace type** | <!-- TODO (Unresolved): composite/derived — no single backing property (see NonTraceableLabels comments) Captured example: "Hybrid" --> *Not available from captured ARM metadata.* |
| **Managed Resource Group** | <!-- TODO (Unresolved): No value match found anywhere in this capture — investigate before adding to any known table (could be a genuine EssentialsExtractor/redaction bug, a portal empty-state placeholder like "---", or a new different-API-surface/composite case). Captured example: "ardl-dbx-managed-xm1ddvc7" --> *Not available from captured ARM metadata.* |
| **URL** | <!-- TODO (Unresolved): No value match found anywhere in this capture — investigate before adding to any known table (could be a genuine EssentialsExtractor/redaction bug, a portal empty-state placeholder like "---", or a new different-API-surface/composite case). Captured example: "https://adb-7405616215055839.19.azuredatabricks.net" --> *Not available from captured ARM metadata.* |
| **Pricing Tier** | <!-- TODO (ShortcutMismatch): Computed "premium" does NOT match portal "Premium (+ Role-based access controls)" for this type — do not trust this shortcut here. Captured example: "Premium (+ Role-based access controls)" --> *Not available from captured ARM metadata.* |
| **Enable No Public IP** | {{ model.props.parameters.enablenopublicip.value | portal_bool_yesno }} |
