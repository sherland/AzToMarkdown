{{~ # Auto-generated by AzResourceDetailsDownloader --generate-templates for Microsoft.DocumentDB/databaseAccounts. ~}}
{{~ # Requires these custom Scriban functions registered on the rendering engine: ~}}
{{~ # portal_bool_enabled, portal_bool_yesno, portal_timestamp, region_display_name — see TemplateFunctions.cs. ~}}
{{~ # Rows marked TODO could not be mechanically resolved against the captured example — see config/portal-field-recipes.json for why. ~}}
# {{ model.name }}

| Property | Value |
|---|---|
| **Status** | *See the Azure Portal for current status.* |
| **Resource group** | {{ model.resource_group }} |
<!-- "Subscription" omitted: tenant/subscription identity, already in frontmatter -->
<!-- "Subscription ID" omitted: tenant/subscription identity, already in frontmatter -->
| **Total throughput limit** | <!-- TODO (Unresolved): composite/derived — no single backing property (see NonTraceableLabels comments) Captured example: "No total throughput limit" --> *Not available from captured ARM metadata.* |
| **Read Locations** | {{ model.props.readlocations[0].locationname }} |
| **Write Locations** | {{ model.props.writelocations[0].locationname }} |
| **URI** | <!-- TODO (NeedsReview): Value matches (exact value match) but the property name is only weakly related to the label (similarity 0.00) — verify by hand before trusting (2 addressable value-matching leaves, none strongly name-related to the label). Captured example: "https://cosmoscibdp-jn.documents.azure.com:443/" --> *Not available from captured ARM metadata.* |
| **Free Tier Discount** | <!-- TODO (Unresolved): composite/derived — no single backing property (see NonTraceableLabels comments) Captured example: "Opted Out" --> *Not available from captured ARM metadata.* |
| **Capacity mode** | <!-- TODO (Unresolved): No value match found anywhere in this capture — investigate before adding to any known table (could be a genuine EssentialsExtractor/redaction bug, a portal empty-state placeholder like "---", or a new different-API-surface/composite case). Captured example: "Serverless" --> *Not available from captured ARM metadata.* |
| **Backup policy** | {{ model.props.backuppolicy.type }} |
