{{~ # Auto-generated by AzResourceDetailsDownloader --generate-templates for Microsoft.DBforPostgreSQL/flexibleServers. ~}}
{{~ # Requires these custom Scriban functions registered on the rendering engine: ~}}
{{~ # portal_bool_enabled, portal_bool_yesno, portal_timestamp, region_display_name — see TemplateFunctions.cs. ~}}
{{~ # Rows marked TODO could not be mechanically resolved against the captured example — see config/portal-field-recipes.json for why. ~}}
# {{ model.name }}

| Property | Value |
|---|---|
<!-- "Subscription" omitted: tenant/subscription identity, already in frontmatter -->
<!-- "Subscription ID" omitted: tenant/subscription identity, already in frontmatter -->
| **Resource group** | {{ model.resource_group }} |
| **Server name** | <!-- TODO (Unresolved): composite/derived — no single backing property (see NonTraceableLabels comments) Captured example: "pgfd6vwwi6" --> *Not available from captured ARM metadata.* |
| **Location** | {{ model.location | region_display_name }} |
| **Status** | *See the Azure Portal for current status.* |
| **Created on** | <!-- TODO (NotAddressable): Traceable via 'systemdata.createdat', but that's outside properties.* — not reachable as model.props.*. Would need the renderer to expose that section, or the label stays manual. Captured example: "2026-08-16 14:07:21 (UTC)" --> *Not available from captured ARM metadata.* |
| **Endpoint** | <!-- TODO (NeedsReview): Value matches (exact value match) but the property name is only weakly related to the label (similarity 0.00) — verify by hand before trusting. Captured example: "pgfd6vwwi6.postgres.database.azure.com" --> *Not available from captured ARM metadata.* |
| **Virtual endpoint** | {{ model.props.highavailability.mode }} |
| **Administrator login** | {{ model.props.administratorlogin }} |
| **Configuration** | <!-- TODO (Unresolved): composite/derived — no single backing property (see NonTraceableLabels comments) Captured example: "Burstable, B1ms, 1 vCores, 2 GiB RAM, 32 GiB storage" --> *Not available from captured ARM metadata.* |
| **PostgreSQL version** | <!-- TODO (NeedsReview): Value matches (exact value match) but the property name is only weakly related to the label (similarity 0.33) — verify by hand before trusting. Captured example: "16" --> *Not available from captured ARM metadata.* |
| **Availability zone** | {{ model.props.availabilityzone }} |
| **High availability** | {{ model.props.highavailability.mode }} |
| **Next maintenance** | <!-- TODO (Unresolved): No value match found anywhere in this capture — investigate before adding to any known table (could be a genuine EssentialsExtractor/redaction bug, a portal empty-state placeholder like "---", or a new different-API-surface/composite case). Captured example: "---" --> *Not available from captured ARM metadata.* |
