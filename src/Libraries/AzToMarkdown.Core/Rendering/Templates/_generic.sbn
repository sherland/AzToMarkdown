{{- extra_fm = "" -}}
# {{ model.name }}

## ℹ️ Details

| Property | Value |
|---|---|
| **Type** | `{{ model.type }}` |
| **Location** | `{{ model.location | region_display_name }}` |
{{ if model.props.kind -}}
| **Kind** | `{{ model.props.kind }}` |
{{ end -}}
{{ if model.version -}}
| **Version** | `{{ model.version }}` |
{{ end -}}

## 📄 Properties

{{ if (array.size model.props_scalars) > 0 -}}
| Property | Value |
|---|---|
{{ for p in model.props_scalars -}}
| **{{ p.key }}** | `{{ p.value }}` |
{{ end }}
{{ else -}}
*No scalar properties.*
{{ end }}
{{ if (array.size model.props_complex) > 0 -}}
**Nested/array properties** (full detail in front matter): {{ for c in model.props_complex }}`{{ c.key }}` ({{ c.kind }}, {{ c.count }}){{ if !for.last }}, {{ end }}{{ end }}

{{ end -}}
## 🔌 Connections
{{ if (array.size model.inbound) == 0 && (array.size model.outbound) == 0 -}}
*No connections detected.*
{{ end -}}
{{ for edge in model.inbound -}}
* **{{ edge.label | string.capitalize }}** ← {{ edge.wiki }}
{{ end -}}
{{ for edge in model.outbound -}}
* **{{ edge.label | string.capitalize }}** → {{ edge.wiki }}
{{ end }}
