{{-
  sku_name = model.props.sku ? model.props.sku.name : null
  extra_fm = sku_name ? "sku_name: " + sku_name : ""
-}}
# {{ model.name }}

## ℹ️ Configuration

| Property | Value |
|---|---|
{{ if sku_name -}}
| **SKU / Tier** | `{{ sku_name }}` |
{{ end -}}

## 🌐 Endpoints & Child Resources

{{ for edge in model.outbound | filter_by_label "child resource" -}}
* {{ edge.wiki }}
{{ end -}}
{{ if !(model.outbound | has_label "child resource") -}}
*No child resources found.*
{{ end }}

## 🛡️ WAF Policies

{{ for edge in model.outbound | filter_by_label "waf policy" -}}
* {{ edge.wiki }}
{{ end -}}
{{ if !(model.outbound | has_label "waf policy") -}}
*No WAF policies attached.*
{{ end }}
